<?php
if ( ! defined( 'ABSPATH' ) || ! is_admin() ) {
	return;
}

if ( !class_exists('MsavWooRemoveProducts_API') ) {

	/**
	 * Class MsavWooRemoveProducts_API API helper
	 */
	class MsavWooRemoveProducts_API {

	}
}